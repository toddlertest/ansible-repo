---
  - name: Roles based AWS playbook
    hosts: local
    connection: local
    becomes: false
    gather_facts: False

    roles:
      - create_aws_instance
  - name: After creation
    hosts: ec2hosts
    becomes: true
    gather_facts: False
    roles:
       - common
       - deploy_jenkins
