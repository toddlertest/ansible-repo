---
  - name: Roles based AWS playbook
    hosts: local
    connection: local
    gather_facts: False

    roles:
      - create_aws_instance
  - name: After creation
    hosts: ec2hosts
    gather_facts: False
    roles:
       - common
       - deploy_jenkins
